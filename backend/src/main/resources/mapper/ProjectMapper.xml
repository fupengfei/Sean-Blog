<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sean.blog.module.project.mapper.ProjectMapper">
    <select id="findPublished" resultType="com.sean.blog.module.project.entity.Project">
        SELECT * FROM t_project WHERE status = 'PUBLISHED' ORDER BY sort_order ASC, created_at DESC
    </select>
    <select id="findFeatured" resultType="com.sean.blog.module.project.entity.Project">
        SELECT * FROM t_project WHERE status = 'PUBLISHED' AND is_featured = 1 ORDER BY sort_order ASC LIMIT #{limit}
    </select>
    <select id="findAll" resultType="com.sean.blog.module.project.entity.Project">
        SELECT * FROM t_project ORDER BY sort_order ASC, created_at DESC
    </select>
    <select id="findById" resultType="com.sean.blog.module.project.entity.Project">
        SELECT * FROM t_project WHERE id = #{id}
    </select>
    <insert id="insert">
        INSERT INTO t_project (id, title, description, url, github_url, cover_image, tags, is_featured, sort_order, status)
        VALUES (#{id}, #{title}, #{description}, #{url}, #{githubUrl}, #{coverImage}, #{tags}, #{isFeatured}, #{sortOrder}, #{status})
    </insert>
    <update id="update">
        UPDATE t_project SET title = #{title}, description = #{description}, url = #{url},
            github_url = #{githubUrl}, cover_image = #{coverImage}, tags = #{tags},
            is_featured = #{isFeatured}, sort_order = #{sortOrder}, status = #{status},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM t_project WHERE id = #{id}
    </delete>
    <update id="updateFeatured">
        UPDATE t_project SET is_featured = #{featured} WHERE id = #{id}
    </update>
    <update id="updateSortOrder">
        UPDATE t_project SET sort_order = #{sortOrder} WHERE id = #{id}
    </update>
</mapper>
